name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  self-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run this action against the fixture recipe
        uses: ./
        with:
          recipe-dir: test/fixtures/minimal
